# ansible

Project for Ansible Automation

1. Install ansible on the local machine.
2. Create an inventory file and put the IP addresses of the servers that Ansible will manage:
   a. Run `nano inventory`.
   b. Create the entries and save the file.


